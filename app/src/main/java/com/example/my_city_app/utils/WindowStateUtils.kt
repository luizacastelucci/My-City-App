package com.example.my_city_app.utils

enum class CityAppContentType {
    ListOnly, ListAndDetail
}

// classe enumerada é utilizada para armazenar valores fixos pré-definidos (constantes)
// essa classe armazena os valores que definem a exibição do conteúdo do app com base no tamanho da tela